# Python cache and build artefacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
build/
dist/

# VS Code settings
.vscode/

# Virtual environments
venv/
.env/
.conda/

# History files
.history/

.DS_Store
._*

# SLEAP project files: labels and predictions. `.slp` (no star) matched only a file
# literally named `.slp`, which is why the 71 MB label file was tracked.
*.slp

# Trained models live in ../sleap-models, not here.
models/

# Per-machine config: the active data-location profile and the local model root.
configs/*.local.yml
